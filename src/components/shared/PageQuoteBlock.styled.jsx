import styled from 'styled-components';

export const PageQuoteBlock = styled.div`
    border-left: 3px solid #ffcc00;
    background: rgba(255, 204, 0, 0.05);
    padding: 1rem 1.25rem;
    border-radius: 0 8px 8px 0;
    max-width: 700px;
    margin: 0 auto 1.75rem;
    color: #ffffff;
    font-size: 1rem;
    line-height: 1.8;
    font-style: italic;
`;

export const QuoteFirstSentence = styled.span`
    color: #ffcc00;
    font-weight: 700;
    font-style: normal;
`;
